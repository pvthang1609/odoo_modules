<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="ir_config_parameter_action" model="ir.actions.act_window">
        <field name="name">Config parameter</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">ir.config_parameter</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="ir_config_parameter_view" model="ir.ui.view">
        <field name="name">List Config parameter</field>
        <field name="model">ir.config_parameter</field>
        <field name="type">tree</field>
        <field name="arch" type="xml">
            <tree string="Config parameter">
                <field name="key"/>
                <field name="value"/>
            </tree>
        </field>
    </record>

    <record id="ir_config_parameter_form" model="ir.ui.view">
        <field name="name">Form Config parameter</field>
        <field name="model">ir.config_parameter</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form>
                <field name="key"/>
                <field name="value"/>
            </form>
        </field>
    </record>

</odoo>